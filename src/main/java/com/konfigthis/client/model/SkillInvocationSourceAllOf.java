/*
 * Box Platform API
 * [Box Platform](https://box.dev) provides functionality to provide access to content stored within [Box](https://box.com). It provides endpoints for basic manipulation of files and folders, management of users within an enterprise, as well as more complex topics such as legal holds and retention policies.
 *
 * The version of the OpenAPI document: 2.0.0
 * Contact: devrel@box.com
 *
 * NOTE: This class is auto generated by Konfig (https://konfigthis.com).
 * Do not edit the class manually.
 */


package com.konfigthis.client.model;

import java.util.Objects;
import java.util.Arrays;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.konfigthis.client.model.FileVersionMini;
import com.konfigthis.client.model.Folder;
import com.konfigthis.client.model.FolderAllOfFolderUploadEmail;
import com.konfigthis.client.model.FolderMini;
import com.konfigthis.client.model.Items;
import com.konfigthis.client.model.ModelFile;
import com.konfigthis.client.model.UserMini;
import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;
import java.io.IOException;
import java.time.OffsetDateTime;
import org.openapitools.jackson.nullable.JsonNullable;

import javax.ws.rs.core.GenericType;

import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;

import com.konfigthis.client.JSON;
@javax.annotation.Generated(value = "Generated by https://konfigthis.com")
public class SkillInvocationSourceAllOf extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(SkillInvocationSourceAllOf.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!SkillInvocationSourceAllOf.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'SkillInvocationSourceAllOf' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<Folder> adapterFolder = gson.getDelegateAdapter(this, TypeToken.get(Folder.class));
            final TypeAdapter<ModelFile> adapterModelFile = gson.getDelegateAdapter(this, TypeToken.get(ModelFile.class));

            return (TypeAdapter<T>) new TypeAdapter<SkillInvocationSourceAllOf>() {
                @Override
                public void write(JsonWriter out, SkillInvocationSourceAllOf value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `Folder`
                    if (value.getActualInstance() instanceof Folder) {
                        JsonObject obj = adapterFolder.toJsonTree((Folder)value.getActualInstance()).getAsJsonObject();
                        elementAdapter.write(out, obj);
                        return;
                    }

                    // check if the actual instance is of the type `ModelFile`
                    if (value.getActualInstance() instanceof ModelFile) {
                        JsonObject obj = adapterModelFile.toJsonTree((ModelFile)value.getActualInstance()).getAsJsonObject();
                        elementAdapter.write(out, obj);
                        return;
                    }

                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: Folder, ModelFile");
                }

                @Override
                public SkillInvocationSourceAllOf read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonObject jsonObject = elementAdapter.read(in).getAsJsonObject();

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize Folder
                    try {
                        // validate the JSON object to see if any exception is thrown
                        Folder.validateJsonObject(jsonObject);
                        actualAdapter = adapterFolder;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'Folder'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for Folder failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'Folder'", e);
                    }

                    // deserialize ModelFile
                    try {
                        // validate the JSON object to see if any exception is thrown
                        ModelFile.validateJsonObject(jsonObject);
                        actualAdapter = adapterModelFile;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'ModelFile'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for ModelFile failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'ModelFile'", e);
                    }

                    if (match == 1) {
                        SkillInvocationSourceAllOf ret = new SkillInvocationSourceAllOf();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonObject));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for SkillInvocationSourceAllOf: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonObject.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, GenericType> schemas = new HashMap<String, GenericType>();

    public SkillInvocationSourceAllOf() {
        super("oneOf", Boolean.FALSE);
    }

    public SkillInvocationSourceAllOf(Folder o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public SkillInvocationSourceAllOf(ModelFile o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("Folder", new GenericType<Folder>() {
        });
        schemas.put("ModelFile", new GenericType<ModelFile>() {
        });
    }

    @Override
    public Map<String, GenericType> getSchemas() {
        return SkillInvocationSourceAllOf.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * Folder, ModelFile
     *
     * It could be an instance of the 'oneOf' schemas.
     * The oneOf child schemas may themselves be a composed schema (allOf, anyOf, oneOf).
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof Folder) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof ModelFile) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be Folder, ModelFile");
    }

    /**
     * Get the actual instance, which can be the following:
     * Folder, ModelFile
     *
     * @return The actual instance (Folder, ModelFile)
     */
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `Folder`. If the actual instance is not `Folder`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `Folder`
     * @throws ClassCastException if the instance is not `Folder`
     */
    public Folder getFolder() throws ClassCastException {
        return (Folder)super.getActualInstance();
    }

    /**
     * Get the actual instance of `ModelFile`. If the actual instance is not `ModelFile`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `ModelFile`
     * @throws ClassCastException if the instance is not `ModelFile`
     */
    public ModelFile getModelFile() throws ClassCastException {
        return (ModelFile)super.getActualInstance();
    }


 /**
  * Validates the JSON Object and throws an exception if issues found
  *
  * @param jsonObj JSON Object
  * @throws IOException if the JSON Object is invalid with respect to SkillInvocationSourceAllOf
  */
  public static void validateJsonObject(JsonObject jsonObj) throws IOException {
    // validate oneOf schemas one by one
    int validCount = 0;
    ArrayList<String> errorMessages = new ArrayList<>();
    // validate the json string with Folder
    try {
      Folder.validateJsonObject(jsonObj);
      validCount++;
    } catch (Exception e) {
      errorMessages.add(String.format("Deserialization for Folder failed with `%s`.", e.getMessage()));
      // continue to the next one
    }
    // validate the json string with ModelFile
    try {
      ModelFile.validateJsonObject(jsonObj);
      validCount++;
    } catch (Exception e) {
      errorMessages.add(String.format("Deserialization for ModelFile failed with `%s`.", e.getMessage()));
      // continue to the next one
    }
    if (validCount != 1) {
      throw new IOException(String.format("The JSON string is invalid for SkillInvocationSourceAllOf with oneOf schemas: Folder, ModelFile. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonObj.toString()));
    }
  }

 /**
  * Create an instance of SkillInvocationSourceAllOf given an JSON string
  *
  * @param jsonString JSON string
  * @return An instance of SkillInvocationSourceAllOf
  * @throws IOException if the JSON string is invalid with respect to SkillInvocationSourceAllOf
  */
  public static SkillInvocationSourceAllOf fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, SkillInvocationSourceAllOf.class);
  }

 /**
  * Convert an instance of SkillInvocationSourceAllOf to an JSON string
  *
  * @return JSON string
  */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

